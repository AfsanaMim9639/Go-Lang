Text Template কী?

👉 Text Template হলো এমন একটা টুল,
যার মাধ্যমে টেক্সটের ভেতরে ভ্যারিয়েবল বা ডেটা ইনসার্ট করতে পারো
একটা নির্দিষ্ট “template” (ছাঁচ) ব্যবহার করে।

Go তে এটি আসে text/template প্যাকেজ থেকে।

| বিষয়              | ব্যাখ্যা                   | উদাহরণ                              |
| ----------------- | -------------------------- | ----------------------------------- |
| **Template তৈরি** | `template.New("name")`     | `template.New("t1")`                |
| **Parse করা**     | টেমপ্লেট টেক্সট রূপান্তর   | `.Parse("Hi {{.Name}}")`            |
| **Execute করা**   | ডেটা বসিয়ে আউটপুট          | `.Execute(os.Stdout, data)`         |
| **Data type**     | struct, map, slice ইত্যাদি | `data := map[string]string{...}`    |
| **if/else**       | শর্ত যাচাই                 | `{{if .Pass}}OK{{else}}Fail{{end}}` |
| **range**         | লুপ চালানো                 | `{{range .Items}} {{.}} {{end}}`    |
| **FuncMap**       | Custom function যোগ করা    | `Funcs(funcMap)`                    |
| **Error handle**  | Must ব্যবহার               | `template.Must(...)`                |


✅ Real-life ব্যবহার

1.ইমেইল বা রিপোর্ট টেমপ্লেট তৈরি

2.Configuration ফাইল ডাইনামিক বানানো

3.CLI প্রজেক্টে আউটপুট জেনারেট করা

4.Go ওয়েব অ্যাপে Dynamic পেজ তৈরি (HTML version সহ)


| অংশ                                   | কাজ                                         |                                                    |
| ------------------------------------- | ------------------------------------------- | -------------------------------------------------- |
| `{{.Name}}`                           | Struct থেকে নাম বসানো                       |                                                    |
| `{{with .Address}}...{{end}}`         | Nested struct (Address) হ্যান্ডল করা        |                                                    |
| `{{if .Passed}}...{{else}}...{{end}}` | শর্ত দিয়ে পাশ বা ফেল দেখানো                 |                                                    |
| `{{range .Subjects}}...{{end}}`       | Slice লুপ করে বিষয়গুলোর নাম ও মার্কস দেখানো |                                                    |
| `{{.Name                              | upper}}`                                    | Custom FuncMap ফাংশন দিয়ে বড় হাতের অক্ষরে রূপান্তর |
| `{{if gt (len .Subjects) 2}}`         | ইনলাইন ফাংশন ব্যবহার (greater than)         |                                                    |
| `template.Must()`                     | Error হ্যান্ডলিং সহজ করা                    |                                                    |
